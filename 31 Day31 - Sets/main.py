# It does not print repeated values
# It can print values in any order,order does not main in sets

a = {2,4,5,2}
print(a)

info = {'fahad',22,'developer',True}
# print(info)

for value in info:
    print(value)

# b = {}
# print(type(b)) It will print class dictionary because same syntax in dictionary also like sets

# if we want to really print type or create empty set then like this we can create see below
c=set()
print(c)